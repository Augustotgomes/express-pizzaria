<!DOCTYPE html>
<html lang="en">
    <%- include('includes/head', {titulo:"Cadastro de Pizza"})%>
<body>
    <%- include('includes/header')%>

    <main class="container">
        <div class="row">
            <div class="col-12">
                <h1>Cadastro de Usuário</h1>
            </div>
            <div class="col-12">
                <form class="w-50" action="/usuarios/cadastro" 
                method="POST" >
                    <div class="form-group">
                        <label for="nomeUsuario">Nome da Usuário</label>
                        <input class="form-control" name="nomeUsuario"
                         id="nomeUsuario" type="text">
                    </div>
                    <div class="form-group">
                            <label for="emailUsuario">Email</label>
                            <input class="form-control" name="emailUsuario"
                             id="emailUsuario" type="email">
                    </div>
                    <div class="form-group">
                            <label for="senhaUsuario">Senha</label>
                            <input class="form-control" name="senhaUsuario"
                             id="senhaUsuario" type="password">
                    </div>

                    <div class="form-group">
                        <button class="btn btn-success">Finalizar Cadastro</button>
                    </div>
                </form>
            </div>
        </div>
    </main>
    
</body>
</html>